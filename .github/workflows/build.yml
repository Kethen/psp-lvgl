name: Build package

on: [ push, pull_request, workflow_dispatch ]

jobs:
  build_pkg:
    runs-on: ubuntu-24.04
    steps:

    - name: Checkout
      uses: actions/checkout@v5
      with:
        submodules: true

    - name: Set env vars
      run: |
        echo "sha_name=$(echo ${GITHUB_SHA} | cut -c1-8)" >> $GITHUB_ENV
        echo "PSPDEV_TAG=ghcr.io/pspdev/pspdev:v20260201" >> $GITHUB_ENV

    - name: Build
      run: |
        docker run --rm -i -v ./:/work_dir -w /work_dir "$PSPDEV_TAG" make
        mkdir -p PSP/GAME/lvgl_demo
        mv EBOOT.PBP PSP/GAME/lvgl_demo/
        mv PARAM.SFO PSP/GAME/lvgl_demo/

    - name: Push package artifact
      uses: actions/upload-artifact@v4
      with:
        name: lvgl_demo_${{ env.sha_name }}
        path: |
           # due to https://github.com/actions/upload-artifact/issues/174, need a dummy file
           .github/artifact.txt
           PSP/GAME/lvgl_demo/EBOOT.PBP
           PSP/GAME/lvgl_demo/PARAM.SFO
        if-no-files-found: error
